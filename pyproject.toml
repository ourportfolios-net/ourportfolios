[project]
name = "ourportfolios"
version = "0.4.0"
description = "An investment strategy analyzing platform"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "apscheduler>=3.11.0",
    "dotenv>=0.9.9",
    "psycopg2>=2.9.10",
    "sqlalchemy>=2.0.41",
    "ruff>=0.12.8",
    "vnstock>=3.2.6",
    "psycopg2-binary>=2.9.10",
    "reflex>=0.8.16",
    "asyncpg>=0.31.0",
]

[tool.semantic_release]
assets = []
build_command_env = []
commit_message = "{version}\n\n was automatically generated by python-semantic-release"
commit_parser = "conventional"
logging_use_named_masks = false
major_on_zero = false
allow_zero_version = true
version_toml = ["pyproject.toml:project.version"]
upload_to_release = true
upload_to_pypi = false
tag_format = "{version}"

[tool.semantic_release.branches.main]
match = "(main|master)"
prerelease_token = "rc"
prerelease = false

[tool.semantic_release.branches.feature]
match = "feat/.*"


[tool.semantic_release.changelog]
exclude_commit_patterns = []
mode = "update"
insertion_flag = "<!-- version list -->"
template_dir = "templates"

[tool.semantic_release.changelog.default_templates]
changelog_file = "CHANGELOG.md"
output_format = "md"
mask_initial_release = true

[tool.semantic_release.commit_parser_options]
minor_tags = ["feat"]
patch_tags = ["fix",  "ci", "perf"]
other_allowed_tags = ["build", "chore", "docs", "style", "refactor", "test"]
allowed_tags = ["feat", "fix", "perf", "build", "chore", "ci", "docs", "style", "refactor", "test"]
default_bump_level = 0
parse_squash_commits = true
ignore_merge_commits = true

[tool.semantic_release.remote]
name = "origin"
type = "github"
ignore_token_for_push = false
insecure = false

[tool.ruff]
exclude = ["**/*.ipynb"]
